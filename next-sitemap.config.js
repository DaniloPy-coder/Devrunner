/** @type {import('next-sitemap').IConfig} */
const config = {
    siteUrl: 'https://exporunner.com.br',
    generateRobotsTxt: true,
    changefreq: 'weekly',
    priority: 0.7,
};

export default config;
